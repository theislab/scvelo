language: python
cache: pip
python:
  - 3.6

install:
  - pip install flit
  - flit install

env: MPLBACKEND=Agg

script:
  - pytest
  - python setup.py check --restructuredtext --strict
  - rst2html.py --halt=2 README.rst >/dev/null
